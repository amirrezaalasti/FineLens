# FineLens backend — direct dependencies (see pyproject.toml for project metadata)
# Install: pip install -r requirements.txt
# Or editable: pip install -e .

# Web API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# Config & validation
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# LLM & knowledge graph
openai>=1.60.0
graphiti-core[falkordb]>=0.29.0
falkordblite>=0.1.0

# HTTP clients & legal data ingestion
httpx>=0.28.0

# Document upload / PDF parsing (chat router)
pypdf>=5.0.0
pymupdf>=1.24.0
